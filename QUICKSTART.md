# 🚀 Quick Start Guide - Cinema Booking System

## Быстрый старт для разработки

### 1. Запуск приложения

```bash
# Если у вас установлен Maven
mvn spring-boot:run

# Или через Java
mvn clean package
java -jar target/booking-cinema-0.0.1-SNAPSHOT.jar
```

### 2. Доступ к приложению

- **Главная страница:** http://localhost:8080/
- **H2 Console:** http://localhost:8080/h2-console
  - JDBC URL: `jdbc:h2:mem:testdb`
  - Username: `sa`
  - Password: _(оставьте пустым)_

### 3. Тестовые учетные записи

После запуска автоматически создаются тестовые данные:

**Администратор:**
- Username: `admin`
- Password: Admin123 (note: BCrypt hashed in database)

**Обычный пользователь:**
- Username: `user`
- Password: `User1234`

### 4. Основные функции

#### Для всех пользователей:
1. 📝 Регистрация нового аккаунта
2. 🔐 Вход в систему
3. 🎬 Просмотр фильмов
4. 🎫 Бронирование билетов
5. 📋 Просмотр своих бронирований

#### Для администраторов:
1. ⚙️ Доступ к админ-панели
2. ➕ Добавление новых фильмов
3. 🗑️ Удаление фильмов
4. 📅 Создание сеансов
5. 🗑️ Удаление сеансов

---

## API Endpoints

### Публичные (без авторизации)

```bash
# Регистрация
POST /api/users/register
Content-Type: application/json
{
  "username": "newuser",
  "email": "user@example.com",
  "password": "StrongPass123",
  "phone": "+7 999 123 45 67"
}

# Вход
POST /api/users/login
Content-Type: application/json
{
  "username": "admin",
  "password": "Admin123"
}
```

### Требуется авторизация (USER или ADMIN)

```bash
# Получить список фильмов
GET /api/v1/films
Authorization: Bearer YOUR_JWT_TOKEN

# Получить фильм по ID
GET /api/v1/films/1
Authorization: Bearer YOUR_JWT_TOKEN

# Создать бронирование
POST /api/v1/bookings
Authorization: Bearer YOUR_JWT_TOKEN
Content-Type: application/json
{
  "session": {"id": 1},
  "customerName": "Иван Иванов",
  "seatNumbers": "A1, A2, A3"
}
```

### Только для ADMIN

```bash
# Добавить фильм
POST /api/v1/films
Authorization: Bearer ADMIN_JWT_TOKEN
Content-Type: application/json
{
  "title": "Новый фильм",
  "genre": "Боевик",
  "duration": 120
}

# Удалить фильм
DELETE /api/v1/films/1
Authorization: Bearer ADMIN_JWT_TOKEN

# Обновить фильм
PUT /api/v1/films/1
Authorization: Bearer ADMIN_JWT_TOKEN
Content-Type: application/json
{
  "title": "Обновленное название",
  "genre": "Комедия",
  "duration": 95
}
```

---

## Пошаговый сценарий использования

### Сценарий 1: Регистрация и бронирование билета

1. Откройте http://localhost:8080/
2. Нажмите "Регистрация"
3. Заполните форму:
   - Username: `testuser`
   - Email: `test@example.com`
   - Password: `TestPass123` (важно: заглавная буква + цифра!)
   - Phone: _(опционально)_
4. Нажмите "Зарегистрироваться"
5. Вы будете перенаправлены на главную страницу
6. Нажмите "Смотреть афишу" или прокрутите вниз
7. Выберите фильм и нажмите "Выбрать сеанс"
8. Выберите сеанс и заполните данные бронирования
9. Подтвердите бронирование

### Сценарий 2: Администрирование (добавление фильма)

1. Войдите как администратор:
   - Username: `admin`
   - Password: Admin123 (note: BCrypt hashed in database)
2. В навигации появится кнопка "⚙️ Админ-панель"
3. Нажмите на неё
4. В разделе "Добавить новый фильм":
   - Название: `Интерстеллар`
   - Жанр: `Научная фантастика`
   - Длительность: `169`
5. Нажмите "Добавить фильм"
6. Фильм появится в списке
7. Перейдите на вкладку "📅 Сеансы"
8. Создайте сеанс для добавленного фильма

---

## Требования к паролям

⚠️ **ВАЖНО:** Новые правила валидации паролей!

Пароль должен содержать:
- ✅ Минимум 8 символов
- ✅ Минимум 1 заглавную букву (A-Z)
- ✅ Минимум 1 строчную букву (a-z)
- ✅ Минимум 1 цифру (0-9)

**Примеры валидных паролей:**
- `Password123` ✅
- `MySecure1Pass` ✅
- `Cinema2024!` ✅

**Примеры невалидных паролей:**
- `password` ❌ (нет заглавной буквы и цифры)
- `12345678` ❌ (нет букв)
- `Pass1` ❌ (меньше 8 символов)

---

## Переменные окружения (Production)

Для production окружения установите следующие переменные:

```bash
# Linux/Mac
export JWT_SECRET="your-very-strong-secret-key-at-least-32-characters-long"
export JWT_EXPIRATION="86400000"
export ADMIN_USERNAME="admin"
export ADMIN_PASSWORD="YourStrongAdminPassword123"

# Windows
set JWT_SECRET=your-very-strong-secret-key-at-least-32-characters-long
set JWT_EXPIRATION=86400000
set ADMIN_USERNAME=admin
set ADMIN_PASSWORD=YourStrongAdminPassword123
```

---

## Структура проекта

```
src/
├── main/
│   ├── java/com/rizat/cinema/
│   │   ├── config/           # Конфигурация (Security, JWT)
│   │   ├── controller/       # REST Controllers
│   │   ├── dto/              # Data Transfer Objects (НОВОЕ!)
│   │   ├── exception/        # Обработка исключений (НОВОЕ!)
│   │   ├── model/            # JPA Entities
│   │   ├── repository/       # Spring Data Repositories
│   │   ├── service/          # Business Logic
│   │   └── util/             # Утилиты (ValidationUtil - НОВОЕ!)
│   └── resources/
│       ├── static/           # HTML, CSS, JS
│       │   ├── index.html    # Главная страница (ОБНОВЛЕНО!)
│       │   ├── admin.html    # Админ-панель
│       │   ├── login.html    # Вход
│       │   ├── register.html # Регистрация
│       │   └── ...
│       ├── application.properties  # Конфигурация (ОБНОВЛЕНО!)
│       └── data.sql          # Тестовые данные
```

---

## Troubleshooting

### Проблема: "Invalid credentials" при входе

**Решение:** Убедитесь, что используете правильный пароль:
- Для `admin`: `Admin123`
- Для `user`: `User1234`

### Проблема: "Password must be at least 8 characters..."

**Решение:** Новый пароль должен соответствовать требованиям:
- Минимум 8 символов
- Содержит заглавные и строчные буквы
- Содержит хотя бы одну цифру

### Проблема: "Email already exists"

**Решение:** Используйте другой email или удалите базу данных:
```bash
# База H2 в памяти, просто перезапустите приложение
# Или очистите target/ директорию
rm -rf target/
```

### Проблема: Фильмы не загружаются

**Решение:**
1. Проверьте, что вы авторизованы
2. Откройте консоль разработчика (F12) и проверьте ошибки
3. Проверьте, что токен не истек (проверьте заголовок `X-Token-Expired`)

### Проблема: CORS ошибка

**Решение:** Убедитесь, что ваш origin добавлен в `SecurityConfig.java`:
```java
configuration.setAllowedOrigins(Arrays.asList(
    "http://localhost:8080",
    "http://localhost:3000",
    "http://your-domain.com"  // Добавьте ваш домен
));
```

---

## Полезные команды

```bash
# Очистить и пересобрать проект
mvn clean package

# Запустить с профилем dev
mvn spring-boot:run -Dspring-boot.run.profiles=dev

# Запустить тесты
mvn test

# Создать JAR файл
mvn clean package
# Файл будет в target/booking-cinema-0.0.1-SNAPSHOT.jar

# Запустить JAR с переменными окружения
JWT_SECRET="my-secret" java -jar target/booking-cinema-0.0.1-SNAPSHOT.jar
```

---

## Дополнительные ресурсы

- 📖 [IMPROVEMENTS.md](./IMPROVEMENTS.md) - Подробное описание всех улучшений
- 📖 [README.md](./README.md) - Общая документация проекта
- 🔗 [Spring Boot Documentation](https://spring.io/projects/spring-boot)
- 🔗 [JWT.io](https://jwt.io/) - Декодер JWT токенов

---

**Enjoy! 🎬🍿**
