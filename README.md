# 🎬 Cinema Booking System - REST API

Система бронирования билетов в кинотеатр на базе Spring Boot с JWT аутентификацией и PostgreSQL.

## 📋 Содержание
- [Технологии](#технологии)
- [Требования](#требования)
- [Установка и запуск](#установка-и-запуск)
- [API Endpoints](#api-endpoints)
- [Регистрация и авторизация](#регистрация-и-авторизация)
- [Примеры использования](#примеры-использования)
- [Структура проекта](#структура-проекта)

## 🚀 Технологии

- **Java 21**
- **Spring Boot 3.3.4**
- **Spring Security** - JWT аутентификация
- **Spring Data JPA** - работа с базой данных
- **PostgreSQL** - база данных
- **BCrypt** - хеширование паролей
- **Maven** - управление зависимостями
- **Lombok** - генерация кода
- **JUnit 5 & Mockito** - тестирование

## ⚙️ Требования

Перед запуском убедитесь, что у вас установлено:

- **Java 21** или выше
- **Maven 3.8+**
- **PostgreSQL 12+**
- **Git**

## 📦 Установка и запуск

### Шаг 1: Клонировать репозиторий

```bash
git clone <your-repository-url>
cd booking-cinema
```

### Шаг 2: Настроить PostgreSQL

1. Установите PostgreSQL, если еще не установлен
2. Создайте базу данных:

```bash
# Войдите в PostgreSQL
psql -U postgres

# Создайте базу данных
CREATE DATABASE cinema_booking;

# Выйдите из psql
\q
```

### Шаг 3: Настроить application.properties

Файл конфигурации находится в `src/main/resources/application.properties`

**Важные параметры для изменения:**

```properties
# Настройки базы данных
spring.datasource.url=jdbc:postgresql://localhost:5432/cinema_booking
spring.datasource.username=postgres
spring.datasource.password=your_password  # Измените на ваш пароль PostgreSQL

# JWT секретный ключ (ОБЯЗАТЕЛЬНО измените в production!)
jwt.secret=cinema_booking_secret_key_change_this_in_production_must_be_at_least_256_bits

# Время жизни токена (в миллисекундах, по умолчанию 24 часа)
jwt.expiration=86400000
```

### Шаг 4: Собрать проект

```bash
mvn clean install
```

### Шаг 5: Запустить приложение

**Вариант 1: Через Maven**
```bash
mvn spring-boot:run
```

**Вариант 2: Через Java**
```bash
java -jar target/cinema-booking-0.0.1-SNAPSHOT.jar
```

**Вариант 3: Через IDE (IntelliJ IDEA, Eclipse)**
- Откройте проект в IDE
- Запустите класс `CinemaBookingApplication.java`

Приложение запустится на `http://localhost:8080`

## 🔐 Регистрация и авторизация

### Как работает система аутентификации

1. **Регистрация**: пользователь регистрируется с username, email, password (и опционально phone)
2. **Хеширование**: пароль хешируется с помощью **BCrypt** перед сохранением в БД
3. **Авторизация**: при успешном логине выдается **JWT токен**
4. **Доступ**: токен используется для доступа к защищенным endpoints

### Модель пользователя (User)

```json
{
  "username": "john_doe",
  "email": "john@example.com",
  "phone": "+79991234567",
  "password": "securePassword123",
  "role": "USER"
}
```

**Поля:**
- `username` - обязательное, уникальное
- `email` - обязательное, уникальное, валидный email
- `phone` - опциональное
- `password` - обязательное (хешируется автоматически)
- `role` - по умолчанию "USER", может быть "ADMIN"

### Регистрация нового пользователя

**POST** `/api/users/register`

```bash
curl -X POST http://localhost:8080/api/users/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "john_doe",
    "email": "john@example.com",
    "phone": "+79991234567",
    "password": "securePassword123"
  }'
```

**Успешный ответ (200 OK):**
```json
{
  "id": 1,
  "username": "john_doe",
  "email": "john@example.com",
  "phone": "+79991234567",
  "role": "USER"
}
```

**Обработка ошибок:**
- `400 Bad Request: "Username already exists"` - username уже занят
- `400 Bad Request: "Email already exists"` - email уже зарегистрирован
- `400 Bad Request` - ошибки валидации (пустые поля, неверный формат email)

### Авторизация (Login)

**POST** `/api/users/login`

```bash
curl -X POST http://localhost:8080/api/users/login \
  -H "Content-Type: application/json" \
  -d '{
    "username": "john_doe",
    "password": "securePassword123"
  }'
```

**Успешный ответ (200 OK):**
```
eyJhbGciOiJIUzUxMiJ9.eyJzdWIiOiJqb2huX2RvZSIsImlhdCI6MTcwNjI4MDAwMCwiZXhwIjoxNzA2MzY2NDAwfQ...
```

**Это JWT токен**, который нужно использовать для всех защищенных запросов.

**Обработка ошибок:**
- `401 Unauthorized: "Invalid credentials"` - неверный username или password

### Использование JWT токена

Для доступа к защищенным endpoints добавляйте токен в заголовок `Authorization`:

```bash
curl -X GET http://localhost:8080/api/films \
  -H "Authorization: Bearer <your-jwt-token>"
```

## 📡 API Endpoints

### Публичные endpoints (без авторизации)

| Метод | Endpoint | Описание |
|-------|----------|----------|
| POST | `/api/users/register` | Регистрация нового пользователя |
| POST | `/api/users/login` | Авторизация и получение JWT токена |

### Защищенные endpoints (требуется авторизация)

#### Фильмы (Films)

| Метод | Endpoint | Роль | Описание |
|-------|----------|------|----------|
| GET | `/api/films` | USER, ADMIN | Получить список всех фильмов |
| GET | `/api/films/{id}` | USER, ADMIN | Получить фильм по ID |
| POST | `/api/films` | ADMIN | Создать новый фильм |
| PUT | `/api/films/{id}` | ADMIN | Обновить фильм |
| DELETE | `/api/films/{id}` | ADMIN | Удалить фильм |

#### Сеансы (Sessions)

| Метод | Endpoint | Роль | Описание |
|-------|----------|------|----------|
| GET | `/api/sessions` | USER, ADMIN | Получить список всех сеансов |
| GET | `/api/sessions/film/{filmId}` | USER, ADMIN | Получить сеансы для фильма |
| POST | `/api/sessions` | ADMIN | Создать новый сеанс |

#### Бронирования (Bookings)

| Метод | Endpoint | Роль | Описание |
|-------|----------|------|----------|
| POST | `/api/bookings` | Authenticated | Создать бронирование |
| GET | `/api/bookings/user/{userId}` | Authenticated | Получить бронирования пользователя |

## 🧪 Примеры использования

### 1. Регистрация и вход

```bash
# Регистрация
TOKEN=$(curl -s -X POST http://localhost:8080/api/users/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "admin",
    "email": "admin@cinema.com",
    "password": "admin123",
    "role": "ADMIN"
  }')

# Логин и получение токена
TOKEN=$(curl -s -X POST http://localhost:8080/api/users/login \
  -H "Content-Type: application/json" \
  -d '{
    "username": "admin",
    "password": "admin123"
  }')

echo "JWT Token: $TOKEN"
```

### 2. Создание фильма (требуется роль ADMIN)

```bash
curl -X POST http://localhost:8080/api/films \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Inception",
    "genre": "Sci-Fi",
    "duration": 148
  }'
```

### 3. Просмотр всех фильмов

```bash
curl -X GET http://localhost:8080/api/films \
  -H "Authorization: Bearer $TOKEN"
```

### 4. Создание сеанса (требуется роль ADMIN)

```bash
curl -X POST http://localhost:8080/api/sessions \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "film": {"id": 1},
    "startTime": "2025-11-15T19:00:00",
    "hall": "Hall 1",
    "totalSeats": 100,
    "availableSeats": 100
  }'
```

### 5. Бронирование билета

```bash
curl -X POST http://localhost:8080/api/bookings \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "session": {"id": 1},
    "user": {"id": 1},
    "seatNumber": 42
  }'
```

## 📁 Структура проекта

```
booking-cinema/
├── src/
│   ├── main/
│   │   ├── java/com/rizat/cinema/
│   │   │   ├── CinemaBookingApplication.java   # Точка входа
│   │   │   ├── config/                         # Конфигурация безопасности и JWT
│   │   │   │   ├── JwtUtil.java
│   │   │   │   ├── JwtAuthenticationFilter.java
│   │   │   │   └── SecurityConfig.java
│   │   │   ├── controller/                     # REST контроллеры
│   │   │   │   ├── UserController.java
│   │   │   │   ├── FilmController.java
│   │   │   │   ├── SessionController.java
│   │   │   │   └── BookingController.java
│   │   │   ├── model/                          # JPA сущности
│   │   │   │   ├── User.java
│   │   │   │   ├── Film.java
│   │   │   │   ├── Session.java
│   │   │   │   └── Booking.java
│   │   │   ├── repository/                     # JPA репозитории
│   │   │   │   ├── UserRepository.java
│   │   │   │   ├── FilmRepository.java
│   │   │   │   ├── SessionRepository.java
│   │   │   │   └── BookingRepository.java
│   │   │   ├── service/                        # Бизнес-логика
│   │   │   │   ├── FilmService.java
│   │   │   │   ├── SessionService.java
│   │   │   │   └── BookingService.java
│   │   │   └── exception/                      # Обработка исключений
│   │   │       └── GlobalExceptionHandler.java
│   │   └── resources/
│   │       ├── application.properties          # Конфигурация приложения
│   │       └── application.properties.example  # Пример конфигурации
│   └── test/                                   # Тесты
├── pom.xml                                     # Maven конфигурация
└── README.md                                   # Документация
```

## 🔒 Безопасность

### Хеширование паролей

Приложение использует **BCrypt** для безопасного хеширования паролей:
- Пароли НИКОГДА не хранятся в открытом виде
- При регистрации пароль автоматически хешируется
- При авторизации пароль сравнивается через BCrypt

### JWT токены

- Токены подписываются секретным ключом (HS512)
- Время жизни токена настраивается через `jwt.expiration`
- Токены содержат username и время создания/истечения
- Для production обязательно смените `jwt.secret`!

### Роли пользователей

- **USER** - обычный пользователь (может просматривать фильмы и бронировать билеты)
- **ADMIN** - администратор (может создавать/изменять/удалять фильмы и сеансы)

## 🐛 Исправленные ошибки

В процессе настройки были исправлены следующие проблемы:

1. ✅ **Method name mismatch в BookingController** - исправлено несоответствие имени метода
2. ✅ **Отсутствие поля phone в User** - добавлено опциональное поле телефона
3. ✅ **Отсутствие валидации email** - добавлена проверка уникальности email при регистрации
4. ✅ **Отсутствие application.properties** - созданы файлы конфигурации
5. ✅ **Улучшена обработка ошибок** - добавлены понятные сообщения об ошибках

## 📝 Переменные окружения

Для production рекомендуется использовать переменные окружения:

```bash
export SPRING_DATASOURCE_URL=jdbc:postgresql://your-host:5432/cinema_booking
export SPRING_DATASOURCE_USERNAME=your_username
export SPRING_DATASOURCE_PASSWORD=your_password
export JWT_SECRET=your_super_secret_key
export JWT_EXPIRATION=86400000
```

## 🧪 Тестирование

Запуск тестов:

```bash
mvn test
```

## 📄 Лицензия

MIT License

## 👥 Контакты

При возникновении вопросов создавайте issue в репозитории.
